<% if message.valid? %>
document.querySelector(".ui-message[data-id='<%= message.id %>']").remove()
<% else %>
alert("No se ha podido marcar el mensaje como leido. Volvé a intentar y si el error persiste contactá a soporte.")
<% end %>
