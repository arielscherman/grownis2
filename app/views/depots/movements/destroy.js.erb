<% if movement.destroyed? %>
document.getElementById("depots-movements-list-placeholder").innerHTML = "<%= j render "depots/movements/index/list", depot: depot, movements: movements, currency: currency %>";
<% else %>
alert("No se ha podido eliminar el movimiento. Volvé a intentar y si el error persiste contactá a soporte.")
<% end %>
